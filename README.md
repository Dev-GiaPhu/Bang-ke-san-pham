# GP Statistical

Firebase Authentication + Cloud Firestore cho dữ liệu realtime; Google Drive chỉ là nguồn đọc file.

Sau khi tạo Firestore, Firebase Console → Firestore Database → Rules: dán nội dung `firestore.rules` và Publish.
