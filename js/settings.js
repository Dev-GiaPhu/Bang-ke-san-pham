import{shell,$,requireAuth,auth,db,doc,getDoc,setDoc,updateDoc,esc}from'./common.js';import{updatePassword,updateEmail}from'https://www.gstatic.com/firebasejs/12.8.0/firebase-auth.js';document.body.innerHTML=shell('settings','Cài đặt','Quản lý tài khoản và thông báo email.');requireAuth(async u=>{const r=doc(db,'users',u.uid),s=await getDoc(r),p=s.data()||{};$('#content').innerHTML=`<div class="grid grid2"><section class="card"><h2>Tài khoản</h2><form class="form" id="account"><label>Tên hiển thị<input class="input" id="name" value="${esc(p.name||'')}"></label><label>Email<input class="input" value="${esc(u.email)}" disabled></label><button class="btn primary">Lưu tên</button></form></section><section class="card"><h2>Đổi mật khẩu</h2><form class="form" id="password"><input class="input" id="newpass" type="password" minlength="8" placeholder="Mật khẩu mới"><button class="btn primary">Đổi mật khẩu</button></form><p id="msg" class="muted"></p></section></div><section class="card" style="margin-top:18px"><h2>Thông báo email</h2><div class="form">${['Chia sẻ bảng kê mới','Thay đổi quyền truy cập','Có chỉnh sửa trên bảng kê'].map((x,i)=>`<label><input type="checkbox" data-notify="n${i}" ${p.notifications?.['n'+i]!==false?'checked':''}> ${x}</label>`).join('')}</div></section>`;$('#account').onsubmit=async e=>{e.preventDefault();await setDoc(r,{name:name.value},{merge:true});alert('Đã lưu')};$('#password').onsubmit=async e=>{e.preventDefault();try{await updatePassword(u,newpass.value);msg.textContent='Đã đổi mật khẩu.'}catch(x){msg.textContent='Vui lòng đăng nhập lại trước khi đổi mật khẩu.'}};document.querySelectorAll('[data-notify]').forEach(x=>x.onchange=async()=>{const n=p.notifications||{};n[x.dataset.notify]=x.checked;await updateDoc(r,{notifications:n})})})